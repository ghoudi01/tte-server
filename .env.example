# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173,https://tte-web.vercel.app

# Database
# Use a local SQLite file for development. Use a managed database URL in production.
DATABASE_URL=file:./dev.db

# Auth
# Generate with: openssl rand -base64 48
JWT_SECRET=replace-with-a-long-random-secret
